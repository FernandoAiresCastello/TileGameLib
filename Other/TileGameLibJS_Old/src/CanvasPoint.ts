export class CanvasPoint {
    
    readonly x: number;
    readonly y: number;
    readonly index: number;

    constructor(x: number, y: number, index: number) {
        this.x = x;
        this.y = y;
        this.index = index;
    }
}
